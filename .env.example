# Copy to .env and fill in real values. Never commit .env.

# Django
SECRET_KEY=your-secret-key-here-generate-with-python-secrets-module
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8001

# Database (shared k8s PostgreSQL at 192.168.86.201:30004 for local dev)
POSTGRES_DB=history-explorer
POSTGRES_USER=your-db-user
POSTGRES_PASSWORD=your-db-password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Logging
LOG_LEVEL=INFO

# S3 Media Storage (optional — omit to use local filesystem)
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
